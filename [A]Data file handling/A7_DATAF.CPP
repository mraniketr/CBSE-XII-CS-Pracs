#include<fstream.h>
#include<conio.h>
#include<stdio.h>

class student
{
int rollno,marks[3] , total;
char name[30];
public:
 void input()
 {
 cout<<"enter r no" ;
 cin>>rollno;
 cout<<"enter nam,e" ;
 gets(name) ;
 }
void calculate()
{
for(int i=0; i<3; i++)
{
cout<<"enter marks " ;
cin>>marks[i];
}
}
void sum()
{
total=0;
total=marks[0]+marks[1]+marks[2];
}
void display()
{
cout<<rollno;
cout<<endl;
puts(name);
cout<<endl;
cout<<total;
}
};
void main()
{
int i;
student s[3];
for(i=0; i<3; i++)
{
s[i].input();
s[i].calculate();
s[i].sum();
s[i].display();
}
ofstream fo;
fo.open("student.dat",ios::out|ios::binary);
for(i=0; i<3; i++)
{
fo.write((char*)&s[i],sizeof(s[i]));
}
fo.close();
ifstream fi;
fi.open("student.dat",ios::in|ios::binary);
for(i=0; i<3; i++)
{
fi.read((char*)&s[i],sizeof(s[i])); }
s[i].display();
getch();
}





































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































