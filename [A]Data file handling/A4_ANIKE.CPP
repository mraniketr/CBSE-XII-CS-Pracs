#include<fstream.h>
#include<conio.h>
#include<stdio.h>
       #include<stdlib.h>
       #include<ctype.h>
main()
{
system("cls");
int c1=0,c2=0;
ofstream fo("sports.txt");
cout<<"enter message";
char a[102];
gets(a);
fo<<a;
puts(a);
fo.close();
ifstream fi("sports.txt") ;
char ch;
while(! fi.eof())
{
fi.get(ch) ;//get(ch) can also be used
if(islower(ch))
c1++           ;
if(isupper(ch))
c2++          ;
}
cout<<"count of l case"<<c1;
cout<<"count of u case"<<c2;

getch() ;
return 0;
}





































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































