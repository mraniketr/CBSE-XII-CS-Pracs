#include<iostream.h>
#include<conio.h>

void selesort(int a[],int n)
{
	     int j;
  for(int i=0; i<n; i++)
 {
 int small, pos , temp ; small=a[i] ; pos=i;
 for(j=i+1; j<n; j++)
 {
  if (a[j]<small)
  { small=a[j];
   pos=j;
   }
  }
   temp=a[i];
    a[i]=a[pos];
    a[pos]=temp;
    for(int k=0; k<n; k++)

    {
     cout<<a[k]<<" ";
     }
     cout<<endl;
    }
}
void bubsort(int a[],int n)
{
    int i,temp;
for(i=0; i<n; i++)
{
 for(int j=0; j<(n-1)-i; j++)
 {
  if(a[j]>a[j+1])
  {
   temp=a[j];
   a[j]=a[j+1];
   a[j+1] =temp;
  }

 }
  for(int k=0; k<n; k++ )
  cout<<a[k]<<" ";
  cout<<endl;

 }
}

main()
{
int a[10] ;
int n, i ,temp ; char xx;
clrscr();
do
{
cout<<"enter array size ";
cin>>n;
cout<<"enter elements";
for(i=0; i<n; i++)
{
cin>>a[i];
}

cout<<"1. bub sort \n 2.sele sort";
cout<<"enter ch" ;
int ch;
cin>>ch;
if(ch==1)
bubsort(a,n);
if(ch==2)
selesort(a,n);
cout<<"do u wanna continue";
cin>>xx  ;
}while(xx=='y'||xx=='Y');
getch();
return 0;
}










































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































